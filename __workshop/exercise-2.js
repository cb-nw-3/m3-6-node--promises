// Exercise 2 - `getAddressPosition`
// ---------------------------------
const dotenv = require("dotevn");
dotenv.config();

function getAddressPosition(address) {
  const requestObj = {
    key: "<MY_API_KEY>",
    q: address,
  };
}
